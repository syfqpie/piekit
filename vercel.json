{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "docs/dist"
      }
    }
  ],
  "env": {
    "PNPM_HOME": ".pnpm"
  },
  "buildCommand": "sleep 60 && pnpm install --frozen-lockfile && pnpm --filter ./docs update piekit && pnpm --filter ./docs build"
}
  